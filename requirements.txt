# Pip-installable dependencies for this repo (non-ROS).
#
# IMPORTANT:
# - ROS2 Python deps (e.g. rclpy, rosbag2_py, geometry_msgs, mavros_msgs, etc.)
#   are NOT installed via pip in a normal venv; they come from your ROS2 installation.
# - This file is for offline analysis/utilities + the ohcoach tools.

PyYAML>=6.0
pyserial>=3.5
pynmea2>=1.19.0

numpy>=1.23
pandas>=1.5

pytz>=2023.3
timezonefinder>=6.2
XlsxWriter>=3.1

requests>=2.31
boto3>=1.28

matplotlib>=3.7

# ROS2 bag parsing without ROS (used by bag_to_human_csv.py / gps_fix_analysis.py)
rosbags>=0.9.0

# NOTE: `ohcoach-cell-tools/` is not packaged for pip install (no setup.py/pyproject.toml).
# If you need `import ohcoach_cell_tools`, run from repo root with:
#   PYTHONPATH="$PWD/ohcoach-cell-tools:$PYTHONPATH" python3 your_script.py
