; Human-readable scenario descriptions for limo_scenario_motion.py
; Format: INI (stdlib configparser), ordered sections run top-to-bottom.
;
; IMPORTANT: Unified schema
; - Every scenario section MUST contain the same set of keys, even if unused.
; - Unused keys should be left blank: `key =`
;
; Units (if present):
; - heading_deg: degrees
; - distance_m: meters
; - speed_mps: meters/second
; - acc_mps2: meters/second^2
; - max_speed_mps: meters/second
; - vel_tolerance: meters/second
; - acc_tolerance: meters/second^2
; - rate_hz: Hz
; - max_duration_s / planned_time_s: seconds
; - final_speed_mps: meters/second (informational)

[meta]
type = const_vel
default_rate_hz = 20
default_max_duration_s = 45
default_vel_tolerance = 0.05
default_acc_tolerance = 0.1
default_max_speed_mps =
inter_run_pause_s = 3.0

[level_1]
scenario = const_vel
heading_deg =
distance_m = 20.0
radius_m =
speed_mps = 0.1
acc_mps2 =
max_speed_mps =
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 240
planned_time_s = 200
final_speed_mps =
notes = 0.1 m/s for ~200s (distance = 20.0 m)

[level_2]
scenario = const_vel
heading_deg =
distance_m = 20.0
radius_m =
speed_mps = 0.55
acc_mps2 =
max_speed_mps =
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 60
planned_time_s = 36.4
final_speed_mps =
notes = 0.55 m/s for ~36s (distance = 20.0 m)

[level_3]
scenario = const_vel
heading_deg =
distance_m = 20.0
radius_m =
speed_mps = 1.0
acc_mps2 =
max_speed_mps =
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s =
planned_time_s = 20
final_speed_mps =
notes = 1.0 m/s for ~20s (distance = 20.0 m) [firmware cap]
