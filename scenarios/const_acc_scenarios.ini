; Human-readable scenario descriptions for limo_scenario_motion.py
; Format: INI (stdlib configparser), ordered sections run top-to-bottom.
;
; IMPORTANT: Unified schema
; - Every scenario section MUST contain the same set of keys, even if unused.
; - Unused keys should be left blank: `key =`
;
; Units (if present):
; - heading_deg: degrees
; - distance_m: meters
; - speed_mps: meters/second
; - acc_mps2: meters/second^2
; - max_speed_mps: meters/second
; - vel_tolerance: meters/second
; - acc_tolerance: meters/second^2
; - rate_hz: Hz
; - max_duration_s / planned_time_s: seconds
; - final_speed_mps: meters/second (informational)

[meta]
type = const_acc
default_rate_hz = 20
default_max_duration_s = 45
default_vel_tolerance = 0.05
default_acc_tolerance = 0.1
default_max_speed_mps =
inter_run_pause_s = 3.0

[level_1]
scenario = const_acc
heading_deg =
distance_m = 20.0
radius_m =
speed_mps =
acc_mps2 = 0.02
max_speed_mps = 1.0
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 120
planned_time_s = 45
final_speed_mps = 0.4
notes = Low accel long ramp; 20m requires ~45s ideally (longer if speed is capped below 1.0 m/s)

[level_2]
scenario = const_acc
heading_deg =
distance_m = 20.0
radius_m =
speed_mps =
acc_mps2 = 0.047
max_speed_mps = 1.0
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 90
planned_time_s = 31
final_speed_mps = 0.87
notes = Linear step accel; 20m should complete in ~31s (longer if speed is capped below 1.0 m/s)

[level_3]
scenario = const_acc
heading_deg =
distance_m = 20.0
radius_m =
speed_mps =
acc_mps2 = 0.073
max_speed_mps = 1.0
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 90
planned_time_s = 27
final_speed_mps = 1.0
notes = Linear step accel; reaches 1.0 m/s cap then cruises (20m ~27s)

[level_4]
scenario = const_acc
heading_deg =
distance_m = 20.0
radius_m =
speed_mps =
acc_mps2 = 0.10
max_speed_mps = 1.0
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 90
planned_time_s = 25
final_speed_mps = 1.0
notes = Still much lower than legacy; reaches 1.0 m/s cap earlier, more cruise (20m ~25s)
