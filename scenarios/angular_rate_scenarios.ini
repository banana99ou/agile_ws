; Human-readable scenario descriptions for limo_scenario_motion.py
; Format: INI (stdlib configparser), ordered sections run top-to-bottom.
;
; IMPORTANT: Unified schema
; - Every scenario section MUST contain the same set of keys, even if unused.
; - Unused keys should be left blank: `key =`

[meta]
type = angular_rate
default_rate_hz = 20
default_max_duration_s = 60
inter_run_pause_s = 3.0

[level_1]
scenario = circular
heading_deg =
distance_m = 125.66
radius_m = 20.0
speed_mps = 1.0
acc_mps2 =
max_speed_mps =
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 30
planned_time_s =
final_speed_mps =
notes = 20m radius circle at 1.0m/s (one full lap ~125.7m). Note: run_scenarios_from_files.py may cap distance via --max-dist.

[level_2]
scenario = circular
heading_deg =
distance_m = 62.83
radius_m = 10.0
speed_mps = 1.0
acc_mps2 =
max_speed_mps =
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 30
planned_time_s =
final_speed_mps =
notes = 10m radius circle at 1.0m/s (one full lap ~62.8m). Note: run_scenarios_from_files.py may cap distance via --max-dist.

[level_3]
scenario = circular
heading_deg =
distance_m = 31.42
radius_m = 1.0
speed_mps = 1.0
acc_mps2 =
max_speed_mps =
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 30
planned_time_s =
final_speed_mps =
notes = 5m radius circle at 1.0m/s (one full lap ~31.4m). Note: run_scenarios_from_files.py may cap distance via --max-dist.

[level_4]
scenario = s_curve
heading_deg =
distance_m = 20
radius_m =
speed_mps =
acc_mps2 =
max_speed_mps =
vel_tolerance =
acc_tolerance =
rate_hz =
max_duration_s = 60
planned_time_s =
final_speed_mps =
notes = S-curve (uniformly scaled to fit 50m x 12m): x(t)=11*sin(0.2t), y(t)=5.5*sin(0.4t)  [scale s=0.55 of original]



